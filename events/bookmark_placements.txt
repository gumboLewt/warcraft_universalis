namespace = bookmark_placements

# Dates:
# Rise of the Horde - 546
# First War - 592
# Fall of Stormwind - 597
# Second War - 598
# Beyond the Dark Portal - 600
# The Alliance Splinters - 605
# Third War - 617
# The Frozen Throne - 618
# Rise of the Qiraji - 622
# The Burning Crusade - 625
# Wrath of the Lich King - 626
# Cataclysm - 631
# Mists of Pandaria - 632
# Warlords of Draenor - 633
# Legion - 635

# First War
# Spawn the Orc nations in the Blasted Lands in 590
# Used if the player starts in the Rise of the Horde bookmark

# The Orcish Horde
# These hideous creatures have been sighted in the Blasted Lands, and have begun to form encampments there. Without resistance, they will soon sweep through our lands.
# Defend our lands!
country_event = {
	id = bookmark_placements.1
	title = bookmark_placements.1.title
	desc = bookmark_placements.1.desc
	picture = ORCISH_INVASION_eventPicture
    
    major = yes
    
	trigger = {
        # Only fire for the Rise of the Horde
        NOT = { started_in = 590 }
		tag = A01
        is_year = 590
        NOT = { has_global_flag = bookmark_rise_of_the_horde }
	}
	
	immediate = {
        set_global_flag = bookmark_rise_of_the_horde
    }
	
	option = {
		name = bookmark_placements.1.option.a

        blackrock_spawn = {
            if = {
                limit = { 
                    OR = {
                        owner = { ai = yes }
                        is_empty = yes
                    }
                }
                cede_province = A41
                add_core = A41
                change_religion = religion_ancestral
                change_culture = culture_blackrock
            }
        }
        
        bleeding_hollow_spawn = {
            if = {
                limit = { 
                    OR = {
                        owner = { ai = yes }
                        is_empty = yes
                    }
                }
                cede_province = A41
                add_core = A41
                change_religion = religion_ancestral
                change_culture = culture_bleeding_hollow
            }
        }
        
        bleeding_hollow_spawn = {
            if = {
                limit = { 
                    OR = {
                        owner = { ai = yes }
                        is_empty = yes
                    }
                }
                cede_province = A40
                add_core = A40
                change_religion = religion_fel
                change_culture = culture_bleeding_hollow
            }
        }
        
        twilights_hammer_spawn = {
            if = {
                limit = { 
                    OR = {
                        owner = { ai = yes }
                        is_empty = yes
                    }
                }
                cede_province = A43
                add_core = A43
                change_religion = religion_fel
                change_culture = culture_twilights_hammer
            }
        }
        
        shadow_council_spawn = {
            if = {
                limit = { 
                    OR = {
                        owner = { ai = yes }
                        is_empty = yes
                    }
                }
                cede_province = B88
                add_core = B88
                change_religion = religion_fel
                change_culture = culture_horde
            }
        }
	}
}

# Exodus of Stormwind
# Occurs when Stormwind is reduced to just capital, allows them to take some land in the Hinterlands from Lordaeron

# Restoration of Loch Modan
# When Lordaeron (or an Alliance member) takes back Loch Modan from the orcs, grant it back to Ironforge

# Restoration of Redridge

# Restoration 